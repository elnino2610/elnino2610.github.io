//- demo: true
mixin contact(args)
	include cssmodule
	+module('div', css.module, args)
		- var args = item
		h3(class=css.title)!= set(args, 'title', random(random.title, 50))
		div(class=css.text)!= set(args, 'text', random(random.text, 50))
